{
  "name": "ttl-partner-survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../backend/frontend-dist && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm run start",
    "postinstall": "cd frontend && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
